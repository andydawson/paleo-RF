Rplots.pdf
figures/
output/
writing/
writing.zip
.~lock*

# Run outputs from the non-interp (March/May) runs of 2026-09-16/17, kept on
# disk for reference but not versioned. The scientifically meaningful outputs
# are frozen in tests/anchors/; these are the fitted model objects, raw draws
# and intermediates. REVIEW LATER: delete once the interp pipeline is the
# established path and these are certainly not needed (see docs/cc/
# questions_for_andria_general.md section 6).
data/calibration_mod1_may_bluesky.RDS
data/calibration_mod2_may_bluesky.RDS
data/calibration_mod3_may_bluesky.RDS
data/calibration_mod4_may_bluesky.RDS
data/calibration_mod5_may_bluesky.RDS
data/calibration_mod6_may_bluesky.RDS
data/calibration_mod7_free_bluesky.RDS
data/calibration_mod7_free_may_bluesky.RDS
data/calibration_mod7_may_bluesky.RDS
data/calibration_mod8_bluesky.RDS
data/calibration_mod8_may_bluesky.RDS
data/calibration_model_selected_bluesky.RDS
data/calibration_model_selected_may_bluesky.RDS
data/calibration_modern_lct_bluesky_coarse.RDS
data/ice_fort.RDS
data/paleo_predict_gam_samps_bluesky.RDS
data/paleo_predict_gam_samps_may_bluesky.RDS
output/

# [run-interp] 2026-09-21: the CACK distribution zip. Its contents are unpacked
# and tracked under data/radiative-kernels/CACKv1.0/, so keeping the 126 MB
# archive as well would double the LFS cost for no benefit. Delete it once you
# are happy the unpacked copy is intact.
data/CACKv1.0.zip

# [run-interp] 2026-09-21: products of the interp tail run (scripts 7, 7a, 8) that
# the scripts write into data/ rather than output/. All four are frozen in
# tests/anchors/interp-tail-2026-09-21/ with checksums, so the copies here are
# regenerable scratch. Ignored to keep `git add -A` from committing run output
# over Andria's files; review when the config stage moves these into output/.
data/ALB_diffs_bluesky.RDS
data/alb_interp_preds_diffs_bluesky.RDS
data/ice_fort_diff_old.RDS
data/ice_fort_diff_young.RDS

# [run-interp] 2026-09-21: keep the small summary tables from script 9, which are
# the numbers behind the slide 18 figure. The rest of output/ stays ignored.
!output/forcing/
!output/forcing/*.csv
